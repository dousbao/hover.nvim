# STILL DEVELOPING. BUT USEABLE
## Intro (Goal)
* Provide a lighweight, highly customizable and easy-configured framework for automatic floating window on hover.
* Currently, does not have plan to handle async operations. Which means will not support remote operations such as:
  * Remote LSP request
  * SSH client
